- hosts: demo
  become: true
  tasks:
    - name: create new deployment
      command: kubectl -f /home/ubuntu/deployment.yml
    - name: create new service
      command: kubectl -f /home/ubuntu/service.yml
